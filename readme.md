Popis ulohy:
📝 ТЗ:
Проект «Электронный дневник» Задача:
Создать веб-приложение для фиксации школьных оценок и автоматического расчета среднего балла.

1. Функционал (JS) Данные:
   Хранить информацию в массиве объектов (например:
   [{ 
    subject: 'Математика', 
    grades:[10, 12] 
  }]). 
  Добавление: 
  При клике на кнопку «Добавить» программа берет название предмета (из select) и оценку (из input), обновляя массив. 
  Логика:
   Если предмет уже есть в списке — добавить оценку в существующий массив; если нет — создать новый объект. 
  Расчет: Для каждого предмета цикл должен считать среднее арифметическое всех его оценок. 
  DOM:
   После каждого изменения страница должна перерисовываться (функция render).
2. Структура (HTML) Выпадающий список предметов (<select>). Поле для ввода оценки (<input type="number">). Кнопка отправки. Контейнер (<ul>), куда будут динамически добавляться карточки предметов. Адаптивность:
   карточки не должны «слипаться» на мобильных экранах.
